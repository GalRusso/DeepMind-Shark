You will receive an array of transcript lines. For each line, return a translation in English or a short error explanation.

Ensure:
- Each item in the "translations" array matches the corresponding input line by position.
- Preserve the array length and order.
- Only one of "text" or "error" should be non-null per item.

<input_lines>
{transcript_lines}
</input_lines>